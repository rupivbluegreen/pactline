// Package audit is the hash-chained immutable audit log.
//
// Phase 0: empty. Phase 1: copy-vendors statebound's audit_event_hash
// pattern (see ADR 0003). Real surface arrives with the first
// state-changing entity in Phase 1.
package audit
