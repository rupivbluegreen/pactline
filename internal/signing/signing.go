// Package signing produces and verifies Ed25519-signed bundles.
//
// Phase 0: empty. Phase 1: copy-vendors statebound's signing pattern
// (signed plan/state bundles, key rotation, audit-log signature
// records) per ADR 0003.
package signing
