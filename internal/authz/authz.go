// Package authz exposes RBAC primitives at v1 and Cedar-backed ABAC
// in Phase 6 (per ADR 0003 — Cedar stays).
//
// Phase 0: empty. Phase 1: Resource/Action enums, Require middleware,
// role-on-(user,org) lookups.
package authz
