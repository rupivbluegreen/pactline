// Package document is the Go client for the Python document sidecar.
//
// Phase 0: empty. Phase 1: gRPC client for the proto contract in
// proto/document.proto, exposing parse + DOCX render calls. The Go
// core never imports PDF/DOCX libraries directly (per ADR 0003).
package document
